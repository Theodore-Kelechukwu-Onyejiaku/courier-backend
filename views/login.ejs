<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,600,700,800,900" rel="stylesheet">		
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/style2.css">
</head>
<body>
    <div>
        <center>
            <div style="margin-top: 5%;padding: 5%">
                <h3>Login</h3>
                <div style="color: rgb(238, 83, 11)">
                    <% var error; var message; %>
                    <% if(error){ %>
                     <strong>Error: </strong>   <%= error %>
                    <%} %>
                </div>
                <div style="color:rgb(52, 238, 151)">
                    <% if(message){ %>
                        <strong> </strong>   <%= message %>
                    <%} %>
                </div>
                <form action="/api/user/login" method="POST" autocomplete="on">
                    <div class="form-group d-flex">
                        <i class="text-warning fa fa-envelope fa-3x"></i><span> </span>
                        <input type="email" name="email" class="form-control" placeholder="email">
                      </div> 
                      <div class="form-group d-flex">
                        <i class="text-info fa fa-key fa-3x"></i><span> </span>
                        <input id="password" type="password" name="password" class="form-control" placeholder="password">
                      </div>
                      <input type="submit" class="btn btn-success">
                </form>
            </div>
        </center>
    </div>
</body>
</html>